# 근무표 정리함

## 포함 기능

- 이메일 매직링크 로그인
- 월별 근무표 이미지 저장
- 월별 스케줄 검수 데이터 저장
- 다른 기기에서 로그인 후 월별 근무표 다시 불러오기
- CSV 추출 프롬프트 복사
- CSV 파일 업로드 또는 CSV 붙여넣기
- CSV 데이터를 검수표에 자동 반영
- 이미지 OCR 자동 추출 베타
- 검수표 수동 수정
- 일간 / 주간 / 월간 / 오늘 출근 / 휴무일 보기
- 카톡 공유 텍스트 복사
- 엑셀 다운로드
- 근무 코드 설정

## GitHub 업로드

압축을 풀고 아래 4개 파일만 GitHub 저장소 최상단에 업로드하세요.

```text
index.html
styles.css
app.js
README.md
```

폴더째 올리지 말고 파일만 올리는 것을 추천합니다.

## Supabase 설정 필요 항목

1. Database 테이블 생성 SQL 실행
2. `schedule-images` Storage bucket 생성
3. Storage 정책 SQL 실행
4. Authentication > URL Configuration에 Vercel 주소 등록

## Supabase 연결 정보

`app.js` 상단에 아래 값이 들어 있습니다.

```js
const SUPABASE_URL = "https://fergbabqmwnbkkxjvgkj.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "sb_publishable_4kIgpTwod32qPE4gfzT_mg_d7MWHshv";
```

Secret key는 절대 넣지 마세요.

## 사용 흐름

1. 이메일 입력 후 Google 로그인 보내기
2. 메일에서 Google 로그인 클릭
3. 기준 연도/월 선택
4. 근무표 이미지 업로드
5. 데이터 입력 탭에서 추출 프롬프트 복사
6. 근무표 이미지와 프롬프트로 CSV 추출
7. 앱의 데이터 입력 탭에 CSV 붙여넣기 또는 업로드
8. CSV를 검수표에 반영 후 확인
9. 저장 버튼 또는 상단 저장 버튼 클릭
10. 월별 보관함에서 저장된 월 불러오기

## 주의

- 이메일 매직링크 로그인을 사용하려면 Supabase Redirect URL에 실제 Vercel 주소를 등록해야 합니다.
- 데이터 입력 탭의 프롬프트는 화면에 길게 표시하지 않고 복사 버튼으로 제공합니다.
- 보조 추출은 베타 기능이므로 권장 방식은 CSV를 만든 뒤 앱에 입력하는 방식입니다.
- Supabase Storage bucket 이름은 반드시 `schedule-images`로 만들어야 합니다.


## Google 로그인 적용 안내

이 버전은 기존 정상 작동 버전에서 로그인 방식만 Google OAuth로 변경한 버전입니다.

- GitHub에는 `index.html`, `styles.css`, `app.js`, `README.md` 4개 파일만 업로드하면 됩니다.
- `auth-callback.html`은 사용하지 않습니다.
- Supabase Authentication > Providers > Google에서 Google Provider를 켜고 Client ID / Client Secret을 저장해야 합니다.
- Supabase Authentication > URL Configuration의 Site URL과 Redirect URLs에는 Vercel 앱 기본 주소를 넣습니다.
- Google Cloud OAuth 클라이언트의 승인된 JavaScript 원본에는 Vercel 앱 주소를 넣고, 승인된 리디렉션 URI에는 Supabase Callback URL을 넣습니다.


## v2 UX 반영 사항
- Google 로그인 정상 동작 버전을 기준으로 유지했습니다.
- OCR 보조 탭의 기본 이름 목록을 현재 팀 순서로 교체했습니다.
- 데이터 입력과 입력 후 확인 화면을 더 명확히 분리했습니다.
- OCR은 주 입력 방식이 아닌 보조 기능으로 한구석에 유지했습니다.
- 주간, 월간, 휴무일, 공유·엑셀 화면의 UI를 정리했습니다.
- 공유·엑셀 화면은 선택일 기준 `[월/일 출근 현황]` 형식의 공유문을 우선 표시합니다.

## v2.2 UX patch
- 주간 보기에서 저번주/다음주 이동 버튼을 추가했습니다.
- 월간 캘린더 코드 위치를 중앙 정렬하고 모바일 잘림을 보정했습니다.
- 휴무일 탭에 다음 휴무, 휴무/연차 요약, 연속 휴무 구간을 추가했습니다.
- 코드 설정을 가로로 긴 행 대신 카드형 그리드로 정리했습니다.
- 데이터 확인 표는 모바일에서 이름 열을 고정하고 가로 스크롤 사용성을 보정했습니다.
- OCR 보조 기본 이름을 현재 팀 10명 기준으로 정리했습니다.


## v2.3 업데이트

- 상위 카테고리형 내비게이션 추가: `근무표 보기`, `데이터 관리`, `보관 · 내보내기`
- 하위 메뉴는 선택한 카테고리에 맞춰서만 표시됩니다.
- 참고 디자인처럼 파스텔 배경, 둥근 카드, 부드러운 모바일 UI를 강화했습니다.
- 모바일 데이터 확인 테이블의 이름 열 고정과 월간 캘린더 잘림을 보정했습니다.
- 코드 설정은 긴 가로 박스 대신 미니 카드형 그리드로 정리했습니다.
- OCR 보조 기본 이름은 `이준호 류선협 이상민 김도영 이승호 곽병우 이미현 이다연 김성민 정세환` 순서입니다.


## v2.4 카테고리 탭 복구 패치

- 상위 카테고리(`근무표 보기`, `데이터 관리`, `보관 · 내보내기`)를 눌렀을 때 해당 카테고리의 하위 탭만 보이도록 수정했습니다.
- Gemini 수정 중 모든 하위 탭이 한 줄에 함께 노출되던 문제를 CSS와 JS 양쪽에서 보정했습니다.
- 모바일에서도 상위 카테고리는 3개 버튼, 하위 탭은 선택 카테고리 안에서만 가로 스크롤로 보이도록 정리했습니다.


## v2.4 rollback-safe UI patch
- 로그인 정상 작동 기준 파일에서 UI만 보정했습니다.
- PC 홈 요약 카드를 3열 카드형으로 정리했습니다.
- 주간 보기는 PC에서 7일 카드가 가로로 보이도록 수정했습니다.
- 월간 범례와 카드 색상을 근무=파랑, 휴무=빨강, 연차=노랑으로 분리했습니다.
- 데이터 입력 버튼명을 `검수표에 반영` / `반영 후 저장`으로 명확히 변경했습니다.
- 코드 설정은 PC에서 여러 열 카드 그리드로 표시됩니다.
- OCR 보조의 추출 영역 입력칸이 카드 밖으로 벗어나지 않도록 보정했습니다.


## v2.5 월별 원본 이미지 뷰어
- 플로팅 `원본 근무표 보기` 버튼에서 현재 월뿐 아니라 월별 보관함에 저장된 다른 달 근무표 이미지도 선택해서 볼 수 있습니다.
- 이미지 뷰어 상단에 월 선택 드롭다운을 추가했습니다.
- 현재 선택월을 바꾸지 않고도 다른 달 원본 이미지만 빠르게 확인할 수 있습니다.


## v2.6 Pastel UI 스타일 리프레시
- 현재 레이아웃과 정보 구조는 유지한 채, 레퍼런스 이미지의 감성적인 모바일 앱 무드에 맞게 UI 스타일만 재정리했습니다.
- 크림/아이보리 배경, 화이트/민트/라임/피치 보조 카드, 딥 네이비 CTA, 큰 라운드, 은은한 그림자, 파스텔 블러 포인트를 적용했습니다.
- 버튼은 캡슐형, 카드와 입력창은 rounded rectangle 스타일로 정돈했습니다.
- 기능 로직과 페이지 구조는 변경하지 않았습니다.


## v2.7 Solid pastel refinement
- 카드/박스의 과한 그라데이션을 제거하고 단색 파스텔 톤으로 정리했습니다.
- 탭 선택 후 표시되는 페이지 박스 안쪽 여백을 보강했습니다.
- 원본 근무표 이미지 뷰어는 예전처럼 오른쪽 아래 플로팅 버튼에서 중앙 모달로 열리는 방식으로 되돌리고, 색감과 둥근 모서리만 현재 스타일에 맞췄습니다.


## v2.8 soft-solid consistency fix
- 주요 카드/네모칸 내부의 남은 그라데이션을 제거하고 단색 파스텔 톤으로 통일했습니다.
- 상위/하위 탭 활성 상태에서 텍스트가 흰색으로 바뀌어 안 보이던 문제를 수정했습니다.
- 저장/주요 버튼의 어두운 색을 제거하고 밝은 파스텔 버튼 스타일로 통일했습니다.
- 휴무일의 다음 휴무 카드 날짜/설명 글자색을 진하게 보정했습니다.
- 원본 근무표 이미지 버튼을 스크롤과 무관하게 화면 오른쪽 아래에 fixed로 고정했습니다.
- 원본 근무표 이미지는 화면 위 모달 방식과 월 선택 드롭다운을 유지합니다.


## v2.9 Final soft solid UI correction
- 카드 내부 그라데이션과 장식성 블러를 제거하고 단색 파스텔 톤으로 정리했습니다.
- 과하게 키웠던 페이지 내부 여백은 되돌리고, 캡처에서 지적된 홈 카드 주변은 단색 배경으로 명확하게 보이도록 수정했습니다.
- 상위/하위 탭 선택 상태에서도 텍스트가 흰색으로 바뀌지 않도록 고정했습니다.
- 저장/주요 버튼의 어두운 색을 제거하고 밝은 파스텔 버튼으로 통일했습니다.
- 원본 근무표 이미지 버튼을 앱 페이지 밖으로 이동해 스크롤과 관계없이 오른쪽 아래에 고정되도록 수정했습니다.
- 원본 이미지 뷰어는 화면 위 모달 방식과 월 선택 드롭다운을 유지합니다.


## v2.7 상위 탭 테두리 및 원본 이미지 버튼 색상
- 상위 3개 큰 카테고리 탭의 선택 상태에서 보이던 하늘색/색상 테두리를 제거했습니다.
- 선택 상태는 테두리 대신 단색 파스텔 배경과 은은한 그림자로만 구분합니다.
- 원본 근무표 이미지 플로팅 버튼은 흰색 대신 연한 피치 톤 `#FBEADC`로 변경했습니다.

## v2.8 기능 개선 반영
- 저장하지 않은 변경사항이 있을 때 월 이동 전 저장 확인을 표시합니다.
- CSV 입력 시 미입력, 미등록 코드, 확인필요 값을 미리 알려줍니다.
- 데이터 확인 탭에 `전체 보기`, `미입력만`, `미등록/확인필요`, `휴무/연차` 필터를 추가했습니다.
- 월별 보관함 카드에 이미지/데이터 저장 상태 배지를 표시합니다.
- 코드 설정 탭에 기본 직원 목록 관리와 코드 유형 선택을 추가했습니다.
- 공유·엑셀 탭에 상세형/간단형/이름만 공유 템플릿을 추가했습니다.
- 일간/주간/월간/휴무일 화면에서 보기 기준 사람을 빠르게 바꿀 수 있습니다.
- 휴무일 탭에서 같은 날 함께 쉬는 사람을 확인할 수 있습니다.


## v2.9 검수 목록 압축 표시
- 데이터 확인 탭의 확인 필요 항목이 너무 길어지지 않도록 미등록 코드 요약을 카드형으로 압축했습니다.
- 각 코드별 위치는 펼쳐서 볼 수 있고, 전체 확인 목록은 기본 접힘 상태로 변경했습니다.
- 확인 목록의 `미등록` 배지와 상세 문장 사이 띄어쓰기/가독성을 보정했습니다.

## v3.0 불확실 추정 코드 검수
- 추출 프롬프트에 `AO?`, `BM?`처럼 불확실한 추정값을 표시하는 규칙을 추가했습니다.
- `?`가 붙은 코드는 데이터 확인 탭에서 `불확실` 항목으로 따로 집계됩니다.
- `AO?`처럼 추정값은 유지하고, 원본 확인 후 맞으면 `?`만 지우면 검수 완료됩니다.
- 표 안의 불확실한 셀은 노란색 점선 스타일로 표시됩니다.
- `확인필요`는 판독 불가, `?` 코드는 추정값 있음, 빈칸은 값 없음으로 구분합니다.


## v3.1 코드표 점검 및 Supabase 보정

- 코드 설정 탭에 `현재 코드표 확인`, `Supabase 코드표 확인`, `기본 코드 보정`, `기본 코드로 초기화` 버튼을 추가했습니다.
- Supabase에 저장된 `work_codes`가 예전 상태라 AO/AQ/AT 같은 기본 코드가 빠져 있을 때 앱에서 직접 확인할 수 있습니다.
- `기본 코드 보정`은 기존 사용자 수정값은 유지하고 누락된 기본 코드만 추가합니다.
- `기본 코드로 초기화`는 이미지 하단 코드표 기준 기본값으로 전체 코드표를 되돌립니다.
- 기본 코드에 `OC(사내행사)`, `MH(병가)`를 추가했습니다.
- Supabase RLS 때문에 외부에서 사용자 DB를 직접 보는 대신, 로그인된 앱 안에서 현재 사용자 기준 코드표를 확인하도록 했습니다.


## v2 코드 설정 단순화
- 코드 설정 탭의 기본 버튼을 `코드 추가`, `저장` 중심으로 줄였습니다.
- `현재 코드 확인`, `저장된 코드 확인`, `기본값으로 되돌리기`는 고급 설정 안으로 이동했습니다.
- `기본 코드 보정`은 별도 버튼이 아니라 앱이 자동으로 처리하는 흐름으로 정리했습니다.
- 버튼명을 사용자 기준으로 `현재 코드 확인`, `저장된 코드 확인`, `기본값으로 되돌리기`로 변경했습니다.

## 이번 버전 변경 사항

- 일간 보기의 기본 화면에 근무 타임라인을 추가했습니다.
- PC에서는 시간축을 가로로 보여주고, 직원별 근무 시간을 막대로 표시합니다.
- 모바일에서는 시간축을 세로형 목록으로 전환해 작은 화면에서도 근무 시작/종료 시간을 비교하기 쉽게 했습니다.
- 일간 화면에서 `타임라인` / `출근시간별` 보기 방식을 전환할 수 있습니다.
- `?`가 붙은 불확실 코드도 코드표에 시간 정보가 있으면 타임라인에 노란색 점선 스타일로 표시됩니다.

## 이번 버전 추가 수정 사항

- 모바일 일간 타임라인을 단순 목록형이 아니라 PC 타임라인을 세로로 돌린 형태로 변경했습니다.
- 모바일에서는 시간축이 왼쪽 세로축으로 표시되고, 직원별 근무 시간이 세로 막대로 표시됩니다.
- 모바일 화면에서도 누가 몇 시부터 몇 시까지 겹쳐 근무하는지 비교할 수 있도록 직원별 막대 영역은 좌우 스크롤됩니다.


## v2.16 모바일 일간 타임라인 폭 정리
- 모바일 세로 타임라인의 시간 눈금을 `06:00` 대신 `6`, `7`처럼 짧게 표시합니다.
- 모바일 근무 막대 안에서는 `07:00~16:00` 시간 문구를 숨기고, 이름과 코드만 작게 표시합니다.
- 모바일 직원 막대 폭과 시간축 폭을 줄여 작은 화면에서 더 많은 직원을 한 번에 비교할 수 있게 조정했습니다.
- PC 타임라인 표시는 기존처럼 유지됩니다.

## v2.17 모바일 일간 타임라인 초압축
- 모바일 세로 타임라인의 왼쪽 시간축 폭을 더 줄였습니다.
- 직원별 근무 막대를 더 얇게 조정했습니다.
- 막대 안의 이름/코드 텍스트는 더 작게 표시하고, 필요하면 막대 밖으로 살짝 튀어나와 보이도록 클리핑을 줄였습니다.
- 한 화면에서 더 많은 근무자를 비교할 수 있도록 모바일 전용 가로 폭을 추가로 압축했습니다.


## v2.17 모바일 타임라인 라벨 정리
- 모바일 일간 타임라인에서 왼쪽 시간축 폭을 더 줄였습니다.
- 근무 막대는 더 얇게 유지하고, 이름/코드는 바 옆에 포스트잇 라벨처럼 붙도록 정리했습니다.
- 라벨은 막대 밖으로 표시되어 모바일에서도 근무 시간 비교 공간을 덜 차지합니다.


## v2.19 모바일 일간 타임라인 라벨 압축
- 모바일 세로 타임라인의 왼쪽 시간축과 근무 막대 폭을 더 줄였습니다.
- 포스트잇 라벨을 근무 막대에 더 밀착시켰습니다.
- 라벨 배경 크기는 줄이고, 이름 텍스트는 잘리지 않도록 overflow 처리를 조정했습니다.

## v2.21 모바일 일간 타임라인 레이아웃 정돈
- 모바일 타임라인에서 직원 컬럼을 고정 폭으로 배치해 7~8명 정도가 화면 안에 자연스럽게 모이도록 조정했습니다.
- 타임라인이 화면 전체 가로폭에 강제로 퍼지지 않도록 수정해 오른쪽 여백이 과하게 남는 문제를 줄였습니다.
- 포스트잇 라벨은 근무 바 시작점에 더 밀착되도록 조정하고, 이름이 잘리지 않도록 overflow 처리를 유지했습니다.
- 모바일 설명 문구를 짧게 줄이고, 플로팅 이미지 버튼과 차트가 겹치지 않도록 하단 여백을 추가했습니다.

## v2.22 검수 보조 기능 4종
- 데이터 확인 탭에 `검수 완료` 상태를 추가했습니다. 검수 완료 후 데이터를 수정하면 `재검수 필요` 상태로 바뀝니다.
- 미등록 코드 요약 카드에 `코드로 추가` 버튼을 추가했습니다. 데이터 확인 중 코드 설정 탭으로 이동하지 않고 임시 코드를 바로 등록할 수 있습니다.
- 검수표 수정 전 상태를 최대 20단계까지 임시 저장하고, `되돌리기` 버튼으로 직전 상태를 복구할 수 있습니다.
- `원본과 나란히 보기` 기능을 추가해 원본 근무표 이미지와 검수표를 좌우로 비교할 수 있습니다. 모바일에서는 위아래로 표시됩니다.

## v2.23 저장 상태 / 월별 상태 배지 강화
- 상단 저장 상태 표시를 `저장됨`, `수정됨 · 저장 필요`, `저장 중...`, `불러오는 중...`, `저장 실패`처럼 상태 배지형으로 구분했습니다.
- 저장 상태 영역에 상태 시각, 안내 문구, 색상 구분을 추가해 현재 데이터가 저장됐는지 더 쉽게 확인할 수 있습니다.
- 월별 보관함 카드에 이미지 여부, 데이터 인원, 검수 상태, 확인 필요 항목 수, 저장 위치 배지를 함께 표시합니다.
- 현재 열려 있는 월에 저장하지 않은 변경사항이 있으면 월별 보관함에도 `저장 필요` 배지가 표시됩니다.

## v3.2 추가 기능

이번 버전에서는 3단계/4단계 기능을 한 번에 반영했습니다.

- 날짜 클릭 상세 보기
  - 월간 캘린더에서 날짜를 누르면 같은 화면 아래에 선택일 상세 카드가 표시됩니다.
  - 내 일정, 출근자 수, 휴무자 수, 출근 시간별 인원, 같이 근무하는 사람을 확인할 수 있습니다.
- 휴무자만 보기
  - 휴무일 페이지에 선택일 기준 휴무/연차 인원만 모아보는 영역을 추가했습니다.
  - 이번 달 휴무자 전체 목록도 펼쳐볼 수 있습니다.
- 같이 근무하는 사람 보기
  - 일간 보기와 날짜 상세 보기에서 내 스케줄 기준 같은 출근 시간대, 시간 겹침 인원을 표시합니다.
- 메모 영역 추출
  - OCR 보조 페이지에 메모 영역 추출 기능을 추가했습니다.
  - 좌표를 조정해 이미지 안의 노란 메모/안내 영역을 OCR로 읽고, 결과를 직접 수정해 저장할 수 있습니다.
- 근무 통계
  - 홈 화면에 근무 통계 카드가 추가되었습니다.
  - 엑셀 다운로드 파일에도 `근무 통계` 시트가 포함됩니다.

구글 캘린더 연동은 이번 버전에는 포함하지 않았습니다.


## 이번 패치
- 월간 캘린더의 날짜 클릭 동작을 선택일 상세 카드 표시가 아니라 해당 날짜의 일간 보기 이동으로 되돌렸습니다.


## v4.2 상단 네비게이션 정리
- 상단 큰 외곽 박스 느낌을 제거하고, 상위 기능 카드 3개를 개별 둥근 카드처럼 보이도록 정리했습니다.
- 하위 탭바는 내용 길이에 맞는 pill 형태로 축소해 긴 빈 공간이 남지 않도록 조정했습니다.
- 불러오기 완료 알림바는 연한 하늘색 계열로 다듬고, 계정 카드의 불러오기 버튼에도 블루 보더를 적용했습니다.
